<h2>Edit Comment</h2>

<%= form_with(model: [@comment.post, @comment], local: true) do |form| %>
  <div>
    <%= form.label :body, "Update your comment:" %><br>
    <%= form.text_area :body, rows: 3 %>
  </div>
  <%= form.submit "Update Comment" %>
<% end %>
